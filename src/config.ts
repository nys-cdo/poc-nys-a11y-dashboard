/**
 * Client-side configuration that is safe to publish (the site is static on
 * GitHub Pages, so these are plain constants rather than build variables).
 */

/** Where "Request an accessibility test" sends people: the ITS intake form. */
export const REQUEST_TEST_URL = 'https://on.ny.gov/a11ytest';
